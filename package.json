{
  "name": "hobnob",
  "version": "0.1.0",
  "description": "Back end for a simple user directory API with recommendation engine",
  "main": "src/index.js",
  "scripts": {
    "fix": "eslint src --fix",
    "lint": "eslint src",
    "build": "rimraf dist && babel src -d dist",
    "serve": "npm run build && dotenv -e envs/.env node dist/index.js",
    "serve:test": "npm run build && dotenv -e envs/test.env -e envs/.env node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:e2e": "dotenv -e envs/test.env -e envs/.env cucumber-js -- spec/cucumber/features --require-module @babel/register --require spec/cucumber/steps",
    "watch": "nodemon -w src --exec npm run serve"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run lint"
    }
  },
  "author": "Rupert Ong <rupert.ong@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.1.5",
    "@babel/core": "^7.1.5",
    "@babel/node": "^7.2.0",
    "@babel/preset-env": "^7.1.5",
    "@babel/register": "^7.0.0",
    "cucumber": "^5.0.3",
    "dotenv-cli": "^1.4.0",
    "eslint": "^5.9.0",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-plugin-import": "^2.14.0",
    "husky": "^1.2.0",
    "nodemon": "^1.18.7",
    "rimraf": "^2.6.2",
    "superagent": "^4.0.0"
  },
  "dependencies": {
    "@babel/polyfill": "^7.0.0",
    "body-parser": "^1.18.3",
    "elasticsearch": "^15.2.0",
    "express": "^4.16.4"
  }
}
